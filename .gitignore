.claude/
CLAUDE.md
.env
TWITTER/
BLUESKY/
twitter_state.json

# Generador SEO — salida y datos viven solo en el servidor (no en git)
data/
equipos/
jornadas/
grupos/
historico/
datos/
articulos/
preview-articulos/
sitemap-data.xml
sitemap-articles.xml

# Live tracker — partidos finalizados guardados (solo en el servidor)
live_data/

# Cuentas de usuario — DB SQLite con datos de personas reales (solo en el
# servidor; backup off-site aparte). NUNCA en git.
user_data/

__pycache__/
seo/__pycache__/_grupo_preview.png

# Ancla local de sync del deck con CLAUDE.md (gitignored como CLAUDE.md)
_claudemd_synced.md

# Infraestructura interna (NUNCA en el repo público; viven en /home/ubuntu/infra
# en el servidor y copia local aquí). Ver "Seguridad del repositorio" en CLAUDE.md.
infra/

# Prevención: ficheros de despliegue interno por si alguien los re-crea.
DEPLOYMENT.md
**/deploy.sh
**/*.service
# docs/ = CLAUDE.md partido en notas. Local, NO se publica: lleva IP, email y
# credenciales, lo mismo que se borro del historial en 2026-08-11.
docs/

# Scratch local: exploración de diseño y baseline de paridad (no van al repo).
_design_exploration/
_pm_baseline.json

# Hallazgos rescatados del code-review (local, no al repo público).
_code_review_seo.md

# Obsidian (vault local, no se versiona)
.obsidian/
